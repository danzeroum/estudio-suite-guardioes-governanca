# curador — saídas

- `harness/change-proposals/CP-NNN-*.yaml` — uma proposta por sinal, com a evidência que a
  produziu embutida, os caminhos afetados, o risco e **como se prova** que funcionou.
- `harness/backlog.yaml` — os itens que viram issue, de forma idempotente.

A proposta fica no repositório **mesmo depois de executada**: proposta que se apaga ao ser
cumprida deixa o repositório sem a resposta para "por que isto é assim?".
